using System;

class Program
{
    static void Main(string[] args)
    {
        for (int i = 100; i < 1000; i++)
        {
            int a = i / 100;
            int b = (i / 10) % 10;
            int c = i % 10;

            if (a != b && a != c && b != c)
            {
                Console.WriteLine(i);
            }
        }
    }
}
